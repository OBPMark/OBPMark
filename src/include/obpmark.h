/**
 * \file obpmark.h
 * \brief OBPMark header for top-level definitions. 
 * \author David Steenari (ESA) 
 */
#ifndef OBPMARK_H_
#define OBPMARK_H_

/* stdint.h to be used for integer types in all benchmarks: uint8_t, uint16_t, etc. */
#include <stdint.h>

/* stddef.h for standard types, e.g. size_t */
#include <stddef.h>

#endif // OBPMARK_H

